$(document).ready(function () {

    /**
     * @name UI app
     * @description Init включает phoneMask(), button e.preventDefault(), fancyBox()
     * @param {Function} phoneMask() -  mask for phone in format +7(999) 999-99-99
     * @param {Event} click() on button[type="button"] - no submit form
     * @param {Function} fancyBox() - plugin popup
     *
     */
    ui.init();



});


//= modules/ui.js